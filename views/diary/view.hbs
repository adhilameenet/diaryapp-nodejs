{{> header}}
<div class="container">
    <a class="mt-3 btn btn-outline-primary" href="/dashboard">Back</a>
    <form class="mt-4">
        <div class="form-group mb-1">
            <label for="title">Date</label>
            <input type="text" value="{{ formatedDate }}" class="form-control" id="title" readonly>
        </div>
        <div class="form-group">
            <label for="content">Diary</label>
            <div class="border ps-2">
                <p>{{{diary.sanitizedHtml}}}</p>
            <div>
        </div>
    </form>
</div>